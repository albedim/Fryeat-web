export const API = "http://localhost:8080/api/v_1_8_0";
export const FORGET_PASSWORD_URL = "/change-password/request";

export const formValider = (param) => {
  return param != null && param != "";
}